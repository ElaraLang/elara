module String
import Elara.Prim
def (++) : String -> String -> String
let (++) s1 s2 = 
    match (s1, s2) with
        ("", _) -> s2
        (_, "") -> s1
        _ -> stringCons (stringHead s1) (stringTail s1 ++ s2)