import Prelude
import Elara.Prim

type TestBox a = | TestBox a

type Wrapper f a = | Wrapper (f a)

let mapWrapper f (Wrapper a) = Wrapper (f a)



let main = 
    let i = Wrapper (TestBox 1)
    println (mapWrapper (\x -> x + 1) i)