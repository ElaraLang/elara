import Prelude
import Elara.Prim

type Number = Int


-- Type aliases can contain type parameters too
type Identity s = s

def makeIdentity : a -> Identity a
let makeIdentity x = x

let main = 
    let i = (42 : Number)
    -- i can be used as a normal Int
    let identity = makeIdentity i
    toString (identity + 1) |> println