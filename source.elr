module Test.Main exposing (main, a)
import Core as C exposing map
import Core qualified as C2 exposing (map, filter)

def main : String -> Int -> String
let main argv _ = 
    println "Hello"
    println "World"

let fact n = if n == 0 then 1 else n * fact (n - 1)