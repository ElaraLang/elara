import Prelude
import Option
import Elara.Prim

type Box a = Box a

type Parser a = Parser (String -> Box a)

def any : Parser Int
let any =
    Parser \s -> Box 0

let runParser (Parser f) x = f x
let main = 
    print ((runParser any) "Hello")
