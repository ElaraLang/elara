import Prelude
import Elara.Prim

def l : List Int
let l = [1, 2, 3]

def map : (a -> b) -> List a -> List b
let map f l = 
    match l with
        Nil -> Nil
        Cons x xs -> Cons (f x) (map f xs)

let main = 
    print (map (\x -> x) l)
