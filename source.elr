import Prelude 
import Elara.Prim

def fact : Int -> Int
let fact n = 
    if n == 0 then 1 else n * fact (n - 1)

let part1 input = 
    let chars = stringToList input |> (map charToInt)
    head chars

let main = 
    print (part1 "hello")
