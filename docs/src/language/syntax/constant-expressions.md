# Constant Expressions

For [annotations](./annotations.md), we aim to let users write arbitrary expressions within annotations, however
these must be able to be fully evaluated at compile time, so that the compiler can extract the necessary information from them.

TODO write more
